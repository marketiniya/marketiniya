name: github automation

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    types: [opened, synchronize]
    branches:
      - master

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  github_automation:
    runs-on: ubuntu-latest
    steps:
      - name: 📚 Git Checkout
        uses: actions/checkout@v4

      - name: 👥 Auto-assign PR reviewer(s)
        run: |
          # Get contributors from the repository
          CONTRIBUTORS=$(curl -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            https://api.github.com/repos/${{ github.repository }}/contributors | \
            jq -r '.[].login' | \
            grep -v "${{ github.event.pull_request.user.login }}" | \
            head -2 | \
            jq -R -s -c 'split("\n")[:-1]')

          echo "Assigning reviewers: $CONTRIBUTORS"

          # Assign the contributors as reviewers
          curl -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/requested_reviewers \
            -d "{\"reviewers\":$CONTRIBUTORS}"

      - name: ✅ Workflow Complete
        run: |
          echo "🎉 GitHub automation completed successfully!"
          echo "✅ Reviewers have been automatically assigned"
          echo "💡 To link this PR to an issue, add 'Closes #123' to the PR description"
          echo "🔗 GitHub will automatically link and close the issue when PR is merged"
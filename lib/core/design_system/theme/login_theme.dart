import 'package:flutter/material.dart';

class LoginTheme {
  static const Color primaryColor = Color(0xFF708446);
  static const Color primaryColorDark = Color(0xFF5F6F39);
  static const Color backgroundColor = Colors.white;

  static const double cardElevation = 2.0;
  static const double logoWidth = 48.0;
  static const double logoHeight = 32.0;
  static const double labelHeight = 28.0;

  static const EdgeInsets cardPadding = EdgeInsets.all(16.0);
  static const EdgeInsets bottomPadding = EdgeInsets.only(bottom: 8.0);
  static const EdgeInsets bottomPaddingLarge = EdgeInsets.only(bottom: 16.0);

  static const double buttonSpacing = 8.0;
}
